import React from 'react';
import { View, Text, Button } from 'react-native';

export default function Contato(){
  return(
    <View>
      <Text style={{marginTop:30, textAlign:'center',}}>Pagina Contato</Text>
    </View>
  )
}